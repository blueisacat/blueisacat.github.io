# 自定义快捷键

!!! tip 操作步骤

    1. 安装 `AutoHotKey` 软件。

    2. `CMD` 打开 `shell:startup` 目录。

    3. 拷贝 `脚本` 文件到该目录。

## Chrome

**Chrome.ahk**

```cmd
#MaxThreadsPerHotkey 1
#MaxThreadsBuffer 1
!+c:: {
    Critical
    winTitle := "ahk_exe chrome.exe"
    if !WinExist(winTitle) {
        Run "C:\Program Files\Google\Chrome\Application\chrome.exe"
        WinWait(winTitle, , 5)
    }  else {
        if WinActive(winTitle) {
            WinMinimize(winTitle)
        } else {
            WinActivate(winTitle)
        }
    }
}
```

## IDEA

**IDEA.ahk**

```cmd
#MaxThreadsPerHotkey 1
#MaxThreadsBuffer 1
!+i:: {
    Critical
    winTitle := "ahk_exe idea64.exe"
    if !WinExist(winTitle) {
        Run("D:\Tools\IntelliJ IDEA\bin\idea64.exe")
        WinWait(winTitle, , 5)
    }  else {
        if WinActive(winTitle) {
            WinMinimize(winTitle)
        } else {
            WinActivate(winTitle)
        }
    }
}
```

## VSCode

**VSCode.ahk**

```cmd
#MaxThreadsPerHotkey 1
#MaxThreadsBuffer 1
!+v:: {
    Critical
    winTitle := "ahk_exe Code.exe"
    if !WinExist(winTitle) {
        Run "D:\Tools\Visual Studio Code\Code.exe"
        WinWait(winTitle, , 5)
    }  else {
        if WinActive(winTitle) {
            WinMinimize(winTitle)
        } else {
            WinActivate(winTitle)
        }
    }
}
```
